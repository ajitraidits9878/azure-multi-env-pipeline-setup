# Pipeline: Service2 - PR Validation (STG)
# Trigger: Pull Requests targeting the 'stg' branch
# Purpose: Run build and unit tests before merging to staging

name: Service2 - PR Validation (STG)

pr:
  branches:
    include:
      - stg
  paths:
    include:
      - /src/Service2/*
      - /tests/Service2/*
    exclude:
      - /Azure/*
      - /DevOps/*

pool:
  name: "STG POOL"

workspace:
  clean: all

steps:
  - template: "../../templates/SetupBuildTemplatePR.yml"
    parameters:
      project: "src/Service2/Service2.sln"

  - template: "../../templates/RunUnitTests.yml"
    parameters:
      project: "src/Service2/Service2.sln"
